sudo apt update & sudo apt upgrade -y

sudo adduser danila
sudo usermod -aG sudo danila
su - danila
sudo whoami

danila12345

curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
sudo apt install -y conntrack
sudo install minikube-linux-amd64 /usr/local/bin/minikube

если у тебя обычный Linux без Docker
minikube start --driver=none

kubectl get nodes

Применить:
kubectl create namespace datalake
kubectl apply -f hdfs.yaml

kubectl apply -f spark-master.yaml
kubectl apply -f spark-worker.yaml
kubectl apply -f spark-master-service.yaml


kubectl apply -f clickhouse-deployment.yaml
kubectl apply -f clickhouse-service.yaml


Проверить статус:
kubectl get pods -n datalake
kubectl get svc -n datalake


Создай папку (в одной папке с yaml):
mkdir -p ~/k8s-datalake/nginx
cd ~/k8s-datalake/nginx


kubectl apply -f nginx-sa.yaml
kubectl apply -f nginx-role.yaml
kubectl apply -f nginx-role-binding.yaml
kubectl apply -f nginx-ingress-service.yaml
kubectl apply -f nginx-ingress-controller.yaml


kubectl apply -f clickhouse-ingress.yaml
kubectl apply -f spark-ui-ingress.yaml


Просмотр UI PySpark и Clickhouse:
http://89-111-173-26.nip.io/spark
http://89-111-173-26.nip.io/clickhouse

curl http://89-111-173-26.nip.io/spark
curl http://89-111-173-26.nip.io/clickhouse

Посмотри логи подов:
kubectl logs pod/spark-master-584969547d-5wfjz -n datalake
kubectl logs pod/spark-worker-XXXXX -n datalake


Удаление подов:
kubectl delete deployment spark-master spark-worker -n datalake
kubectl delete deployment spark-ui-ingress -n datalake
kubectl delete pod -l app=spark-ui-ingress -n datalake --force --grace-period=0
kubectl get all -n datalake | grep spark-ui-ingress
kubectl delete svc nginx-ingress -n datalake


kubectl exec -it -n datalake pod/spark-master-7694984cc-rpkc5 -- bash


Полный сброс внутри namespace datalake:
# Удалить ВСЕ ресурсы в datalake
kubectl delete all --all -n datalake

# Удалить ingress, если остались
kubectl delete ingress --all -n datalake

# Удалить PVC и PV, если они были
kubectl delete pvc --all -n datalake
kubectl delete pv --all

# Удалить configmap и secrets, если они были
kubectl delete configmap --all -n datalake
kubectl delete secret --all -n datalake

kubectl delete namespace datalake
kubectl create namespace datalake
